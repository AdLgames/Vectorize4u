# The tracer versions, in one place.
#
# A tracer bump changes output geometry, which moves every benchmark score.
# An unpinned bump shows up months later as an unexplained regression that
# nobody can attribute, so CI, the Docker images and the local setup
# instructions all read these same numbers.
VTRACER_VERSION=0.6.5
RESVG_VERSION=0.48.1
